# 0.4.2 · 接入已确认的钢琴录音

用户已确认《光落在窗台》试听稿。本次将同一MP3原样接入两季，设为默认背景音乐，没有再次改写或重渲染曲目。

## 录音与打包

- 已确认录音：music/drafts/after-the-light/after-the-light.mp3。
- 构建文件：assets/after-the-light-BGFVd-m5.mp3，2,048,939字节。
- SHA-256：c12e2e8c7b6c73ea0a1bf9ebdbd0ed400e0ac768d67f011468b50eb6bd087a59；源文件与构建文件逐字节一致。
- 完整游戏构建11个文件，共4,989,555字节，包含采样署名及CC BY 3.0许可。
- 发布脚本通过base64 Git blob写入MP3，并核对Git blob SHA；只有全部文件/路径/版本/远端状态检查通过后才开始写入。不会把二进制转成UTF-8，也不会上传创作草稿、WAV或采样缓存。

## 播放与验证

- npm test：175项通过。含24项录音播放器生命周期/手机解锁竞态测试，及8项游戏发布文件边界/二进制往返测试。
- 使用一个HTMLAudioElement和一个Web Audio音乐通道流式播放，未调用decodeAudioData创建整首歌的AudioBuffer。
- 首次开始后真实媒体readyState=4、status=playing，播放位置递增，音乐波形非零，曲名为《光落在窗台》。
- 暂停于76.21秒，恢复后从76.55秒继续；关闭独立音乐后master仍开启、录音暂停且音频归零。
- 切季前76.72秒，切到第一季并开始后77.31秒，保持同一首曲目的位置。
- 正式构建在390×844视口首次播放正常；总静音会暂停媒体、输出归零。循环使用原录音的自然尾音，没有声称无缝拼接。
- 原始操作音效保留；不再同时叠加旧示例琶音。错误会在诊断中报告，可通过音符按钮明确重试，不会悄悄换成别的曲子。
- 最终构建浏览器错误日志为空。手机尺寸和Web Audio状态检查不等同于真实手机设备音频认证。

## 发布

Pages构建已完成，游戏11个文件及首页4个文件的公网散列验证全部通过，回执分别保存在本目录。线上MP3散列与获用户确认的试听文件一致。固定入口不变，游戏存档键和关卡revision不变。

游戏提交：`4793cdc1ece490e601f03927e1af3098df2e6d06`。首页提交：`fbea92f72d03a3d7e04ce941cd92b3c56f585716`。
