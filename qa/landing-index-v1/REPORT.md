# 固定游戏首页 · 索引页 v1

## 入口

目标固定地址：https://yuanhongboo.github.io/orbit-breaker/quiet-workshop/

两季各九关的介绍、插画与入口；读取当前浏览器的进度，支持继续最近的未完成关卡、查看已完成的整季成果。提供复制固定地址按钮。入口只读游戏存档，不启动 Three.js / Rapier，也不上传进度。

## 验证

- `npm test`：130 项全部通过（含既有120项、4项入口选择与重玩测试、6项发布边界测试）。见 `tests.txt`。
- 索引页生产构建成功：4个文件，共91,458字节；见 `build-manifest.json`。游戏仍是已发布的0.4.0，本次未修改或重发游戏版本目录。
- 桌面1280×720：两季入口分别到达咖啡角和雨后花盆，页面无横向溢出，见桌面截图。
- 手机390×844：单列卡片、续玩便条及页尾复制按钮正常，无横向溢出。原有测试样例清洁66%，通过索引页“继续上次收拾”进入后仍为66%。测试使用独立4194地址，没有覆盖玩家存档。
- 复制按钮实际操作显示“入口链接已复制”；随后恢复测试前剪贴板。最终构建浏览器错误日志为空。
- 独立审查：游戏发布成功后，串行使用同一hosting receipt构建/发布入口；入口发布前验证目标游戏与引用资源存在。只允许index.html和自有assets文件，不能改写v*/版本目录。

## 发布边界

GitHub Pages 构建完成，4个公网文件的长度和SHA-256均与本地验收构建一致，回执见 `deployment.json`。今后游戏发布流程会更新固定入口；若入口步骤失败，只需单独恢复入口构建/发布。最终Pages提交是入口提交，游戏回执保留游戏提交，二者分别验证。

发布提交：`973483617fabc969d47377ffe4631ff281627bd1`。

内置浏览器直开公网地址时导航超时，因此展示了4188本地同版首页；公网验证通过HTTP实际下载和散列检查完成，交互与手机布局在本地构建验收。
